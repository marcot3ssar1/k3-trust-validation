# Secrets & live credentials — never commit
.env
*.env
# the live registration (with real token) is ignored; the redacted copy in
# evidence/ IS committed on purpose
synapse_registration.json
state/synapse_registration.json
.ringtok
.ghtok

# Python
__pycache__/
*.py[cod]
venv/
.venv/
*.egg-info/

# Runtime data
*.db
/tmp/
